# 自动修复模块 (`auto/`)

## 职责
自动修复策略管理、策略编排、修复执行记录、修复大盘。

## 内部结构
```
auto/
├── routes/     # 4 路由文件
├── services/   # 3 服务文件
│   └── remediationService.ts  ← 核心修复编排（1426 行，需拆分）
```

## 依赖关系
- 依赖 `ai/`（修复建议生成）、`workflow/`（修复流程编排）
- 被 `alerts/` 触发

## 关键说明
- `remediationService.ts`（1426 行）是项目中最大的后端单体文件，待拆分
- 修复策略引擎已设计 HITL（人在回路）审批模式
